-- No sharing, maximal parallelisme; ported from `parallel/parfib/Main.hs`.

#ifndef N
#define N 41u64
#endif

main =
    $show (nfib N);

nfib x =
    if x < 2u64 then 1u64 else nfib (x - 2u64) + nfib (x - 1u64) + 1u64;
